# This template is NOT to be used. This is just for demonstration purposes of the features
# you can use in your own template.

#server {
#    listen   {{ NGINX_PORT }};
#    server_name {{ NGINX_SERVER_NAME }}; 

#    access_log /var/log/nginx/access.log detailed;
#    error_log  /var/log/nginx/error.log warn;


#  {% for location_block in item.location.items() %}
#    {{ location_block }}
#  {% endfor %}

#        root {{ NGINX_ROOT }};
#        rewrite (.*)\.html $1 permanent;
#        index index.html;
#        try_files $uri.html $uri/ /index.html;
#}


